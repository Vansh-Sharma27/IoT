__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

vm_server/models/
*.pt
*.onnx
vm_server/db/photos/
vm_server/db/*.sqlite
vm_server/db/*.sqlite-journal
vm_server/config.local.yaml

# Live config files contain secrets (secret_token) and ephemeral values (vm_url).
# The checked-in config.yaml files are placeholder templates only (CHANGE_ME).
# Edit them locally; do NOT commit real values.
vm_server/config.yaml
pi_client/config.yaml

# Enrollment / training photos — never commit faces.
WIN_*.jpg
WIN_*.jpeg
WIN_*.png
photos/
calibration_photos/

.DS_Store
*.swp
